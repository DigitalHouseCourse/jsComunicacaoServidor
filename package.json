{
  "name": "pacote",
  "version": "1.0.0",
  "description": "api fake",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "servidor": "json-server dados.json"
  },
  "author": "LostPets",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.17.0"
  }
}
